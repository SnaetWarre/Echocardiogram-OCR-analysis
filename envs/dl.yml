# After create: GPU training — `mamba run -n DL pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124`
name: DL
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - numpy<2
  - pyside6
  - pydicom
  - pillow
  - matplotlib
  - ipykernel
  - jupyterlab
  - notebook
  - nbconvert
  - tesseract
  - pytest
  - requests
  - pip:
      - opencv-python
      - pytesseract
      - easyocr==1.7.2
